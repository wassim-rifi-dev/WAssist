import { Send } from "lucide-react";

export default function SubmitButton() {
    return (
        <button type="submit">
            <span>Send</span>
            <Send />
        </button>
    );
}